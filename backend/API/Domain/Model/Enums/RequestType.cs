namespace API.Domain.Model.Enums
{
    public enum RequestType
    {
        ADOPCION = 1,      // ADOPTION
        DONACION = 2,      // DONATION
        VOLUNTARIADO = 4,
        PADRINO = 5
    }
}
