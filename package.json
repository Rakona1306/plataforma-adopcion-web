{
	"devDependencies": {
		"husky": "9.1.7",
		"prettier": "^3.9.6",
		"prettier-plugin-tailwindcss": "^0.8.1"
	},
	"lint-staged": {
		"*.{js,jsx,ts,tsx}": [
			"pnpm exec eslint --fix"
		],
		"*.{json,md,css}": [
			"pnpm exec prettier --write"
		]
	},
	"scripts": {
		"prepare": "husky"
	}
}